{
  "name": "mtga-collection",
  "version": "0.1.0",
  "description": "MTG Arena collection extractor (macOS) – oclif + TypeScript",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "mtga-collection": "bin/run"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./bin/run",
    "prepack": "npm run build",
    "lint": "eslint \"src/**/*.{ts,tsx}\" \"test/**/*.js\"",
    "lint:fix": "npm run lint -- --fix",
    "test": "npm run build && node --test test/unit test/e2e",
    "test:unit": "npm run build && node --test test/unit",
    "test:e2e": "npm run build && node --test test/e2e"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@oclif/core": "^3.26.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "typescript": "^5.6.0"
  },
  "oclif": {
    "bin": "mtga-collection",
    "commands": "./dist/commands"
  }
}
